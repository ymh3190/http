<div class="wrapper-button">
  <% if (id==='user' ) { %>
  <button id="<%= id %>" class="<%= inHtml %>">
    <i class="fa-solid <%= icon %>"></i>
    <span> <%= title %> </span>
  </button>

  <% } else { %>
  <button id="<%= id %>" class="<%= inHtml %>">
    <% if (title) { %>
    <span> <%= title %> </span>
    <% } %>
    <% if (icon) { %>
    <i class="fa-solid <%= icon %>"></i>
    <% } %>
  </button>
  <% } %>
</div>