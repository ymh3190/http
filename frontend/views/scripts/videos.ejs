<script type="application/json" data-js="videos">
  [
    <% videos.forEach((video,i)=> { %>
    <% if (i<videos.length-1) { %> {
      "video": {
        "id": "<%= video.id %>"
      }
    },
    <% return; %>
    <% } %> {
      "video": {
        "id": "<%= video.id %>"
      }
    }
    <% }); %>
  ]
</script>